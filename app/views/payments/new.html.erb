<h1>New payment status</h1>

<p class="info">
  Current payment status are:
  <% @payments.each do |payment| %>
    <strong><%= payment.title %></strong>
    <%= h "," if !@payments.last %>
    
  <% end %>
</p>
<%= render 'form' %>


<%= link_to 'Back', bookings_path %>
