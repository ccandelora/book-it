  <tr>
    <td><%= link_to "Week " + booking.start_date.strftime("%W"), booking %> 
    (<%= booking.start_date.strftime("%d-%m-%Y") + " - " +  booking.end_date.strftime("%d-%m-%Y") %>)
    </td>
    <td><%= link_to booking.tenant.name, booking.tenant %></td>
    <td><%= booking.persons %></td>
    <td><%= booking.booking_type %></td>
    <td>
      <%= Payment.find(booking.payment_id).title %>
    </td>
    <td><%= booking.status %></td>
    <td><%= link_to 'Edit', edit_booking_path(booking) %></td>
  </tr>  